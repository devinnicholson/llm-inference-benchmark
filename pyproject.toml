[project]
name = "llm-inference-benchmark-lab"
version = "0.1.0"
description = "Controlled vLLM experiments for KV-cache behavior, scheduling, and serving performance."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
modal = ["modal>=1.4,<2"]

[tool.pytest.ini_options]
pythonpath = ["src"]
